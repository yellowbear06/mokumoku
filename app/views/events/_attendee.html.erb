<li>
  <div class="d-flex align-items-center gap-2 mb-3">
    <%= image_tag attendee.decorate.avatar, class: 'rounded-circle avatar-md' %>
    <%= link_to attendee.name, user_path(attendee.id) %>
    <% unless current_user == attendee %>
      <%= render partial: 'follows/follow_form', locals: { user: attendee } %>
    <% end %>
  </div>
</li>
